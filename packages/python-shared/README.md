# `packages/python-shared`

Why it exists:
- Shared Python utilities and conventions reused by `apps/api` and `apps/workers`.
- Standardizes logging, errors, configuration patterns, and common helpers.

