# `apps/web`

React + Vite + Tailwind frontend.

Architecture goals:
- Component-based UI (design system + composition)
- Feature-first organization (`src/features/*`)
- Clear separation between UI, state, and API services

No boilerplate is committed yet; this folder is ready for AI-generated scaffolding.

