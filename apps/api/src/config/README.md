## `src/config`

This folder exists to match common backend conventions (**config**).

Canonical configuration lives in `src/core/config/` (typed settings, env parsing).
`src/config/` is reserved for app-level configuration glue (e.g., DI wiring presets) if needed later.

